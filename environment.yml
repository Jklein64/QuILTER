name: guided-filter
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.7
  - numpy
  - pillow
  - pip
  - pip:
      # guided filter
      - opencv-python
      - opencv-contrib-python
